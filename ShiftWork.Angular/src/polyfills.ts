/***************************************************************************************************
 * BROWSER POLYFILLS
 */

/**
 * If your application is using Zone.js and you need to support IE9-11, you need to import the classlist.js polyfill.
 * For more information, see https://angular.io/guide/browser-support
 */
// import 'classlist.js';  // Run `npm install --save classlist.js`.

/**
 * Web Animations `@angular/platform-browser/animations`
 * Only required if AnimationBuilder is used within the application and you need to support IE9-11 and Android4.x.
 * Run `npm install --save web-animations-js`.
 */
// import 'web-animations-js';  // Run `npm install --save web-animations-js`.

/**
 * By default, zone.js will patch all possible API. You can disable some of them by configuring
 * the environment variable `__Zone_disable_IE_CONSTRUCTOR_BUG__` to `true` (for IE) or `__Zone_disable_MEDIA_QUERY__` for mediaQuery
 */

/***************************************************************************************************
 * Zone JS is required by default for Angular itself.
 */
import 'zone.js';  // Included with Angular CLI.


/***************************************************************************************************
 * APPLICATION IMPORTS
 */
